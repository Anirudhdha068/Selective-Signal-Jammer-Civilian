## System Overview

The system is built around an ESP microcontroller that operates as both a wireless controller and a web server.

Users connect to the ESP via its local IP address and interact with the system using a browser-based interface.
